<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Petition App - Home</title>
</head>
{{> head}}
<body>

    {{> header}}

    <div class="container">
        <p>We the undersigned demand that</p>

        {{#each persons}}
        {{#if url}}
        <p> <a href={{url}}>{{firstname}} {{lastname}} </a> ({{age}} <a href='/signers/{{city}}'>{{city}} </a>)</p>
        {{else}}
        <p>{{firstname}} {{lastname}} ({{age}} <a href='/signers/{{city}}'>{{city}}</a>) </p>
        {{/if}}
        {{/each}}

    </div>

</body>
</html>
